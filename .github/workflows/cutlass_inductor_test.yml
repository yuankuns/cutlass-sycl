name: "TorchInductor CUTLASS UTs"
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  merge_group:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  run-cutlass-test:
    name: Run TorchInductor CUTLASS test
    runs-on: bmg
    timeout-minutes: 120
  
    steps:
      - name: Checkout sycl-tla repo
        uses: actions/checkout@v4

      - name: Install Python venv package
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-venv

      - name: Install Intel graphics drivers
        uses: ./.github/actions/install-intel-graphics
        with:
          GPU: 'BMG'
          IGC: 'ROLLING'

      - name: Install MKL
        shell: bash
        run: |
          #if [ -d /home/ghrunner/intel/oneapi/mkl ]; then
          #  echo "MKL already installed at /home/ghrunner/intel/oneapi/mkl"
          #else
          #  echo "Installing MKL..."
          #  if [ -f $PWD/intel-deep-learning-essentials-2025.3.2.36_offline.sh ]; then
          #    echo "DL Installer exists!"
          #  else
          #    sudo wget https://registrationcenter-download.intel.com/akdlm/IRC_NAS/b3e6c1bf-a6d5-4580-8b1d-80cbfd38c8af/intel-deep-learning-essentials-2025.3.2.36_offline.sh
          #  fi
          #  bash intel-deep-learning-essentials-2025.3.2.36_offline.sh -a --action modify --silent --eula accept --components intel.oneapi.lin.mkl.devel
          #  bash intel-deep-learning-essentials-2025.3.2.36_offline.sh -a --action modify --silent --eula accept --components intel.oneapi.lin.dpcpp-cpp-compiler
          #fi
          #rm -rf ~/intel/oneapi/ ~/intel/installercache/ ~/intel/packagemanager/
          ls -l /home/ghrunner/intel
          echo $USER
          echo $HOME
          export ONEAPI_INSTALL_DIR_FOR_PT=$HOME/pt_ut_dle
          wget -q https://registrationcenter-download.intel.com/akdlm/IRC_NAS/b3e6c1bf-a6d5-4580-8b1d-80cbfd38c8af/intel-deep-learning-essentials-2025.3.2.36_offline.sh
          bash intel-deep-learning-essentials-2025.3.2.36_offline.sh -a --silent --eula accept --instance=pt_oneapi --install-dir=$ONEAPI_INSTALL_DIR_FOR_PT
          echo $ONEAPI_INSTALL_DIR_FOR_PT
          ls -lr $ONEAPI_INSTALL_DIR_FOR_PT
          source $ONEAPI_INSTALL_DIR_FOR_PT/mkl/latest/env/vars.sh
          source $ONEAPI_INSTALL_DIR_FOR_PT/setvars.sh
          export TORCH_WHL=$HOME/pytorch_ut/torch-2.13.0a0+git0002a87-cp312-cp312-linux_x86_64.whl
          env >> $GITHUB_ENV

      - name: Setup virtual environment
        shell: bash
        run: |
          python3 -m venv ~/.pytorch-venv
          source ~/.pytorch-venv/bin/activate

      - name: Install nightly PyTorch
        shell: bash
        run: |
          source ~/.pytorch-venv/bin/activate
          python3 --version
          echo $TORCH_WHL
          #python3 -m pip install --pre torch==2.13.0.dev20260521+xpu --index-url https://download.pytorch.org/whl/nightly/xpu --force-reinstall
          python3 -m pip install $TORCH_WHL

      - name: Clone PyTorch fork and install requirements
        shell: bash
        run: |
          source ~/.pytorch-venv/bin/activate
          cd ${{ runner.temp }}
          git clone https://github.com/pytorch/pytorch.git
          cd pytorch
          git checkout 0002a8783abe6623eacc71df6c05d72869cd5ba5
          python3 -m pip install -r requirements.txt
          python3 -m pip install unittest-xml-reporting
          USE_XPU=1 make triton

      - name: Print IGC2 version
        shell: bash
        run: |
          dpkg-query --show libigc2

      - name: Print PyTorch package version
        shell: bash
        run: |
          source ~/.pytorch-venv/bin/activate
          python3 -m pip list | grep -i torch

      - name: Check sycl-ls
        shell: bash
        run: |
          sycl-ls
      
      - name: Check Device compatibility
        shell: bash
        run: |
          source ~/.pytorch-venv/bin/activate
          export USE_XPU=1
          export CUTLASS_USE_SYCL=1
          export ONEAPI_DEVICE_SELECTOR=level_zero:gpu
          export TORCH_XPU_ARCH_LIST=bmg
          export TORCHINDUCTOR_CUTLASS_DIR="${GITHUB_WORKSPACE}"
          python -c "import torch; print(torch.__version__)"
          python -c "import torch; print(torch.xpu.get_device_capability()['device_id'])"
          python3 -m pip list | grep -i triton


      - name: Debug big GPU detection
        shell: bash
        run: |
          source ~/.pytorch-venv/bin/activate
          export USE_XPU=1
          export CUTLASS_USE_SYCL=1
          export ONEAPI_DEVICE_SELECTOR=level_zero:gpu
          export TORCH_XPU_ARCH_LIST=bmg
          export TORCHINDUCTOR_CUTLASS_DIR="${GITHUB_WORKSPACE}"
          python3 - <<'PY'
          import torch
          from torch._inductor.utils import is_big_gpu
          from torch.testing._internal.inductor_utils import HAS_CPU

          print("HAS_CPU =", HAS_CPU)
          print("xpu available =", torch.xpu.is_available())
          if torch.xpu.is_available():
              p = torch.xpu.get_device_properties(0)
              print("device props =", p)
              print("multi_processor_count =", getattr(p, "multi_processor_count", None))
          print("is_big_gpu =", is_big_gpu())
          PY

      - name: Run TorchInductor Backend test
        shell: bash
        run: |
          rm -rf /tmp/torchinductor_*
          source ~/.pytorch-venv/bin/activate
          export USE_XPU=1
          export CUTLASS_USE_SYCL=1
          export ONEAPI_DEVICE_SELECTOR=level_zero:gpu
          export TORCH_XPU_ARCH_LIST=bmg
          export TORCHINDUCTOR_CUTLASS_DIR="${GITHUB_WORKSPACE}"
          mkdir -p ${GITHUB_WORKSPACE}/logs/
          cd ${{ runner.temp }}/pytorch
          python3 test/inductor/test_cutlass_backend.py -v  2>&1 | tee ${GITHUB_WORKSPACE}/logs/cutlass_inductor_backend.log

      - name: Run TorchInductor EVT test
        shell: bash
        run: |
          rm -rf /tmp/torchinductor_*
          source ~/.pytorch-venv/bin/activate
          export USE_XPU=1
          export CUTLASS_USE_SYCL=1
          export ONEAPI_DEVICE_SELECTOR=level_zero:gpu
          export TORCH_XPU_ARCH_LIST=bmg
          export TORCHINDUCTOR_CUTLASS_DIR="${GITHUB_WORKSPACE}"
          mkdir -p ${GITHUB_WORKSPACE}/logs/
          cd ${{ runner.temp }}/pytorch
          python3 test/inductor/test_cutlass_evt.py -v 2>&1 | tee ${GITHUB_WORKSPACE}/logs/cutlass_inductor_evt.log


      - name: Upload logs
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-artifacts
          path: logs/

      - name: Cleanup PyTorch repo and wheel
        if: always()
        shell: bash
        run: |
          rm -rf ${{ runner.temp }}/pytorch
          rm -rf ~/.pytorch-venv
          echo $ONEAPI_INSTALL_DIR_FOR_PT
          rm -rf $ONEAPI_INSTALL_DIR_FOR_PT
          #bash intel-deep-learning-essentials-2025.3.2.36_offline.sh -a --action remove --silent --eula accept --ignore-errors 



